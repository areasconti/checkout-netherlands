{
  "name": "campaign-kit-templates",
  "version": "1.0.0",
  "description": "Starter templates for next-campaign-page-kit (SDK 0.4.x)",
  "scripts": {
    "setup": "npx campaign-init",
    "start": "npx campaign-start",
    "dev": "npx campaign-dev",
    "build": "npx campaign-build",
    "clone": "npx campaign-clone",
    "config": "npx campaign-config",
    "compress": "npx campaign-compress",
    "compress:preview": "npx campaign-compress --preview",
    "lint:sdk": "node scripts/lint-sdk.mjs",
    "lint:sdk:source": "node scripts/lint-sdk.mjs --source",
    "lint:sdk:rendered": "node scripts/lint-sdk.mjs --rendered",
    "lint:sdk:promoted": "node scripts/lint-sdk.mjs --scope=promoted --pages=checkout,select",
    "lint:sdk:promoted:source": "node scripts/lint-sdk.mjs --source --scope=promoted --pages=checkout,select",
    "lint:sdk:promoted:rendered": "node scripts/lint-sdk.mjs --rendered --scope=promoted --pages=checkout,select",
    "lint:sdk:ci": "CI=1 node scripts/lint-sdk.mjs --scope=all --pages=checkout,select"
  },
  "dependencies": {
    "next-campaign-page-kit": "^0.1.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.2.4"
  }
}
